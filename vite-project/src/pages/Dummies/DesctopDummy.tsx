import Box from "@mui/material/Box";
import { theme } from "../../app/ThemeProvider/theme";

const DesctopDummy = () => {
  return (
    <Box
      sx={{
        background: theme.palette.background.default,
        
      }}
    >Здесь будет заглушка</Box>
  );
};

export default DesctopDummy;
